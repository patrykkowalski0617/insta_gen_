import React from "react";
import { TerminalWrapper } from "./Terminal.styles";

const Terminal = ({ children }) => {
  return <TerminalWrapper>{children}</TerminalWrapper>;
};

export default Terminal;
